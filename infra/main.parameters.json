{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "environmentName": {
      "value": "${AZURE_ENV_NAME}"
    },
    "location": {
      "value": "${AZURE_LOCATION}"
    },
    "pythonClientImageName": {
      "value": "${SERVICE_PYTHON_CLIENT_IMAGE_NAME}"
    },
    "sharedRgName": {
      "value": "${SHARED_RG_NAME=shared}"
    },
    "sharedContainerAppsEnvironmentName": {
      "value": "${SHARED_CONTAINER_APPS_ENVIRONMENT_NAME=shared-containerappsenv}"
    },
    "sharedAcrName": {
      "value": "${SHARED_ACR_NAME=sharedklgoyiacr}"
    }
  }
}
